{
  "name": "ai-command-center",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "apps/transcribe"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/transcribe",
    "build": "npm run build -w apps/transcribe",
    "start": "npm run start -w apps/transcribe",
    "lint": "npm run lint -w apps/transcribe"
  }
}
