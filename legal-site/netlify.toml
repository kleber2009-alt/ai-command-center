[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Заполняются в UI Netlify → Site configuration → Environment variables:
# TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID,
# NEXT_PUBLIC_TELEGRAM_USERNAME, NEXT_PUBLIC_WHATSAPP_PHONE
