// /agents/reels — Reels Creator (v0). Live AI agent: niche+topic → full package.

import { ReelsAgent } from "./ReelsAgent";

export const dynamic = "force-dynamic";

export default function ReelsAgentPage() {
  return <ReelsAgent />;
}
